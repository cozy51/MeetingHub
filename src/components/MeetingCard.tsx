import { CheckCircle2, ChevronRight, Circle, Link2, MapPin, Star } from "lucide-react";
import { Category, Meeting } from "@/types";
export default function MeetingCard({meeting:m,category,onClick}:{meeting:Meeting;category?:Category;onClick:()=>void}){
 const done=m.tasks.filter(t=>t.completed).length;
 return <button className="meeting-card" onClick={onClick}><div className="card-date"><strong>{new Date(m.date+"T12:00:00").getDate()}</strong><span>{new Intl.DateTimeFormat("ja-JP",{month:"short",weekday:"short"}).format(new Date(m.date+"T12:00:00"))}</span></div><div className="card-body"><div className="card-title"><h3>{m.title}</h3>{m.important&&<Star className="star" fill="currentColor" size={17}/>}</div><div className="badges"><span className={`badge ${category?.color??"slate"}`}>{category?.name??m.category}</span><span className="place"><MapPin size={14}/>{m.place}</span>{m.tags.slice(0,1).map(t=><span className="tag" key={t}>#{t}</span>)}</div></div><div className="card-meta"><span className={m.links.length?"has":""}><Link2 size={17}/>{m.links.length?`${m.links.length}件のリンク`:"リンクなし"}</span><span className={m.tasks.length?"has":""}>{m.tasks.length?(done===m.tasks.length?<CheckCircle2 size={17}/>:<Circle size={17}/>):<Circle size={17}/>} {m.tasks.length?`${done}/${m.tasks.length} 完了`:"タスクなし"}</span></div><ChevronRight className="card-arrow"/></button>
}
